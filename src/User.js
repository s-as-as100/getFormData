import React from 'react'

export default function User(props) {
     
     return(
         <div>
             <button onClick = {props.data} >function pass as props</button>
         </div>
     )
}
